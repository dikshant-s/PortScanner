services:
  - type: web
    name: port-scanner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mainScanner:app --bind 0.0.0.0:$PORT
    plan: free
    autoDeploy: true
